<h2><%= @project.title %></h2>
<p><%= @project.description %></p>

<div class="link">
	<span>
		<%= link_to 'Edit Project', edit_organization_project_path( @organization, @project ) %>
	</span>
</div>
<%= button_to 'Delete Project', organization_project_path( @project ), confirm: 'Are you sure?', method: :delete %>